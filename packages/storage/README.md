# @docscn/storage

S3-compatible object storage adapter for artifact revision HTML. Works with
MinIO locally and can be pointed at R2, S3, or another compatible provider via
environment variables.

## Building

Run `nx build storage` to build the library.
